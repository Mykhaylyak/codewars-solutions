// https://www.codewars.com/kata/57faa6ff9610ce181b000028
function crap(x, bags, cap){
let counter = 0;
for (let row of x){
  for (let el of row){
    if(el === '@'){
      counter++;
    }
    if(el === 'D')
      return "Dog!!";
  }
}
  return counter <= bags * cap ? "Clean" : "Cr@p";
    
}
